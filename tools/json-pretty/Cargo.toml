[package]
name = "json-pretty"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Small JSON pretty printer designed to be compiled to WebAssembly."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1"
