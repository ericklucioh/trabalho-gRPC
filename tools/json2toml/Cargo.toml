[package]
name = "json2toml"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Small JSON to TOML converter designed to be compiled to WebAssembly."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1"
toml = "0.8"
