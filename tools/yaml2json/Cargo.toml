[package]
name = "yaml2json"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Small YAML to JSON converter designed to be compiled to WebAssembly."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1"
serde_yaml = "0.9"
